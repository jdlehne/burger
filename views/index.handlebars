
<div class="container">
<header>Eat Burgerz</header>

<div class="row">
  <div class="col-md-4" id="eatBurgs">
    <ul style="list-style: none;">
      {{#each burger_name}}
      <div>
        <li>
          <p id="listBurg">
            {{this.id}}.  {{this.burger_name}}
            <form action="/{{this.id}}?_method=DELETE" method="POST">
              <button id="eatBtn" type="submit">Devour</button>
            </form>
          </p>
        </li>
      </div>
      {{/each}}
    </ul>
  </div>

  <div class="col-md-4">
    <img src="/assets/img/8bitburger.jpg">
  </div>

<div class="col-md-4" id="addBurg">
  <h2>Add Burger</h2>
  <form method="POST" action="/" class="button-size">
    <textarea type="text" name="burger_name"></textarea>
    <button type="submit">Add This Burger!</button>
  </form>
</div>

</div><!---end row---->

<div class ="row">
  <div class= "col-md-12" id="addBurg">

      <h2>Add Burger</h2>
      <form method="POST" action="/" class="button-size">
        <textarea type="text" name="burger_name"></textarea>
        <button type="submit">Add This Burger!</button>
      </form>

  </div>
</div>


  <!--<h2>Change Burger</h2>
  <form action="/?_method=PUT" method="POST" class="button-size">
    <select name="id">
		{{#each burger_name}}  <!-needs to reference the object.id from the db--><!--
			<option value="{{this.id}}">{{this.burger_name}}</option>
		{{/each}}
	</select>
    <textarea type="text" name="burger_name" placeholder="burger_name"></textarea>
    <button type="submit">Modify existing Burger</button>
  </form>-->

</div><!--close container-->
